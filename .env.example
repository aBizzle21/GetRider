# Railway sets DATABASE_URL automatically when you reference the Postgres service.
# You only need to set INGEST_TOKEN yourself.
INGEST_TOKEN=change-me-to-a-long-random-string
# DATABASE_URL is provided by Railway — do not set it by hand.
